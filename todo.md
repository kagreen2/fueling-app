# Supplement Feature — Build Checklist

## Database & Backend
- [ ] Create `supplement_library` table (name, brand, category, default_description, thorne_product_url, image_url)
- [ ] Create `supplement_recommendations` table (supplement_id, athlete_id, team_id, coach_id, coach_note, priority, timing, status)
- [ ] Create `athlete_supplement_status` table (recommendation_id, athlete_id, is_taking, started_at, stopped_at)
- [ ] Add RLS policies for all three tables
- [ ] Seed supplement library with common Thorne products

## Coach UI
- [ ] Supplement Library browse/search page (coach can view all available supplements)
- [ ] "Recommend Supplement" flow — pick supplement, choose athlete or team, add note, set priority/timing
- [ ] Coach supplement visibility dashboard — see which athletes are taking what
- [ ] Team-level recommendation support

## Athlete UI
- [ ] Athlete Supplements tab/page — see all recommendations from coach
- [ ] "Taking It" / "Not Taking" toggle per supplement
- [ ] Priority/timing display (Essential/Recommended/Optional + timing guidance)
- [ ] Thorne deep link per product + general Thorne dispensary button
- [ ] Coach's note/purpose displayed per supplement

## Integration
- [ ] Thorne affiliate dispensary link (prominent button)
- [ ] Product-specific deep links where available
- [ ] Notification when coach assigns new supplement (in-app)

## Coach Dashboard — Fuel Score Enhancements

### Completed
- [x] Team Fuel Score Distribution Chart (stacked bar showing zone breakdown)
- [x] Enhanced 7-day trend sparklines per athlete in overview table
- [x] New zone names: Locked In / On Track / Dial It In / Red Flag
- [x] Consistency Streak Bonus (+2 to +5 points for 5+ day streaks)
- [x] Day-over-day Trend Indicator (↑ ↓ → with magnitude)

### Roadmap
- [ ] Team Average Fuel Score Over Time — line chart showing team avg score over 7/14/30 days, useful for correlating with training load cycles

## FUEL 42 Participant Onboarding

- [x] Define a separate challenge pass through October 31, 2026, so FUEL 42 access does not create or distort a $25 recurring subscription.
- [x] Add secure participant-claim APIs, access gating, initial onboarding completion, a Kelly-assigned coach workflow, and an admin FUEL 42 roster.
- [x] Run the production Supabase FUEL 42 enrollment SQL and connect the supplied Gymnetics consultation link.
- [x] Deploy FUEL 42 onboarding routing, discounted-checkout handling, the Gymnetics webhook bridge, and the favicon build workaround to Vercel at commit `b7fc865`.
- [ ] Deploy the app workflow and public confirmation-page booking CTA.
- [x] Enable the approved single-use `FUELFREE2` code on the $139 member checkout and verify that the live Stripe checkout displays an `Add promotion code` field. The final $0 purchase, enrollment, and automation test remains pending until the Gymnetics workflow actions are configured.
- [x] Verify immediately before the live test that the approved single-use `FUELFREE2` promotion code is valid and unused. Stripe showed `0/1` redemptions and no active redemptions.
- [ ] Complete the approved controlled $0 checkout. The code was confirmed valid and unused in the Stripe Dashboard, but both the Link-authentication checkout and the standard non-Link checkout display “This code is invalid.” Inspect the promotion-code and payment-link eligibility configuration before creating a replacement test code.
- [x] Confirm the exact Stripe restriction causing checkout rejection. The `FUELFREE2` promotion code is active, unused, limited to one redemption, and is marked **First-time order only**; the intended test email has prior Stripe order history, so Stripe correctly rejects the code without consuming it.
- [x] Identify the active Stripe promotion-code record associated with the test coupon for troubleshooting. It shows a single permitted redemption, a Sep. 13 expiration, and zero redemptions; no mutation has been made during this investigation.
- [x] Preserve the existing unused `FUELFREE2` code rather than create a replacement. The attempted replacement coupon was never created because Stripe required a selectable product token; no new coupon or promotion code was saved.
- [x] Use a new Stripe customer email identity for the controlled test so the original promotion code’s first-time-order requirement is satisfied. User approved the Gmail plus-address test identity; the previously provided mobile number remains the participant SMS target.
- [ ] Diagnose the remaining Stripe promotion-code eligibility issue. The unused `FUELFREE2` code still displays “This code is invalid” for the approved Gmail plus-address test identity, with Link detail saving disabled. Verify that the coupon’s product/price restriction matches the $139 Payment Link before any checkout is submitted.
- [x] Review Stripe’s official Payment Links guidance. Stripe confirms first-time-order promotion codes do not work as expected for one-time Payment Links because those links create guest customers, explaining the original `FUELFREE2` failure. Promotion codes inherit any product restrictions from their coupon; the unrestricted `FUEL42QA2026` code should otherwise be eligible, so its rejection requires product/price or checkout-session diagnosis rather than another first-time-order workaround.
- [x] Preserve `FUELFREE2` unchanged after it was rejected for the approved Gmail plus-address test identity. The user approved a separate, private fallback test code rather than deleting the original code.
- [ ] Create the user-approved private fallback test coupon/code in Stripe: 100% off, one redemption total, redeemable through Sep. 13 at 11:59 p.m. Central, no first-time-order restriction, and used only for this controlled test. Because Stripe’s product selector is not controllable in the active browser, it will be globally eligible but kept private and capped at one redemption.
- [ ] Complete the fresh private test-coupon form now loaded in Stripe. Do not enable product-specific selection or first-time-order eligibility; retain the approved 100%-off, one-time, date-limited, one-redemption, private-code controls only.
- [ ] Complete the private test coupon configuration: the form is named `FUEL 42 TEST ONLY - Automation Validation`, set to 100% off and once, with product-specific selection off. Set its enabled redemption deadline from today to Sep. 13 at 11:59 p.m. Central, cap coupon redemptions at one, and add the separate private promotion code with first-time-order eligibility left off.
- [x] Set the private FUEL 42 test coupon’s redemption deadline to Sep. 13, 2026 at 11:59 p.m. Central. Product-specific selection and first-time-order eligibility remain off; the test coupon still requires its one-redemption cap and private promotion code before creation.
- [x] Apply the one-redemption cap to the private FUEL 42 test coupon. It remains 100% off, one-time, expires Sep. 13 at 11:59 p.m. Central, has no product selector or first-time-order setting enabled, and is not yet created.
- [x] Stage the private promotion code `FUEL42QA2026` for the one-time test coupon. The first-time-order-only, customer-specific, code-redemption-limit, code-expiration, and minimum-order options are all left disabled; the coupon-level Sep. 13 deadline and single-redemption cap provide the required safeguards.
- [x] Submit and confirm the private FUEL 42 test coupon/code in Stripe. `FUEL42 TEST - Automation QA` is active at 100% off once, shows 0/1 redemptions, expires Sep. 13 at 11:59 p.m. Central, and uses the private code `FUEL42QA2026` without the first-time-order restriction.
- [ ] Inspect the new private promotion-code record because the live $139 Payment Link still reports `FUEL42QA2026` as invalid immediately after creation. No checkout was submitted and the coupon still showed 0/1 redemptions before this attempt. Verify the promotion code itself is active and determine whether the Payment Link is filtering discounts by product/coupon scope.
- [x] Confirm the underlying `FUEL42 TEST - Automation QA` coupon is valid, 100% off once, unused at 0/1, and expires Sep. 13. The attached promotion-code table is still loading; inspect it to confirm whether `FUEL42QA2026` was actually created and active.
- [x] Confirm the attached `FUEL42QA2026` promotion code exists, is active, has zero redemptions, and expires Sep. 13 at 11:59 p.m. Central. Retry the member checkout after the dashboard confirmed activation; if Stripe still rejects it, inspect the Payment Link discount restrictions rather than changing the coupon again.
- [x] Inspect the direct `FUEL42QA2026` promotion-code record: it has no minimum amount, `First-time order only` is No, no customer restriction is shown, zero redemptions, and a Sep. 13 expiration. The code remains unused despite checkout rejection.
- [x] Retry the private code through Stripe’s `prefilled_promo_code` Payment Link parameter; the checkout still reports “This code is invalid” and remains at $139. No checkout, redemption, or enrollment was created.
- [ ] Confirm the promotion code’s active/inactive state through its action menu. If the menu offers Deactivate, it is active and the remaining issue is Stripe’s Payment Link validation rather than code status.
- [ ] Inspect the $139 FUEL 42 Payment Link in the Stripe Dashboard to confirm it belongs to the same account and is configured to accept general promotion codes, including a 100%-off one-time code.
- [ ] Open the Payment Links list through Stripe’s sidebar; the guessed `/payment_links` path redirected to the account dashboard and made no configuration change.
- [x] Confirm the $139 FUEL 42 member Payment Link is active in the same Iron Flag Fitness Westmont Stripe account, has `Allow promotion codes: Yes`, collects full name and phone, has no automatic tax, and redirects to the correct thank-you page. No link restriction visible in its overview explains the code rejection.
- [ ] Inspect the exact Stripe product ID and price ID used by the active $139 Payment Link and compare them with the FUEL 42 member product record. Check for a stale, archived, duplicated, or otherwise mismatched product/price relationship that could invalidate all attempted promotion codes.
- [x] Identify the live FUEL 42 member product as `prod_V99OUKTskZjtvn`. The product record is Active, shows $139, and has the expected member-challenge description; inspect the pricing table after it finishes loading to capture the exact active price ID and any duplicate or archived prices.
- [x] Confirm the member product has exactly one active/default $139 one-time price, `price_1U8r62Fd19nnAKLWhtkBijCF`, created with the product on Aug. 26. There are no duplicate or archived prices on the product, so the repeated promotion-code rejection is not caused by the Payment Link pointing to a stale duplicate price.
- [ ] Use a controlled fallback instead of spending further time on Stripe’s unexplained promotion-code validation: create a private $0 FUEL 42 QA product/price and a Payment Link limited to one completed checkout, collect name/phone, disable tax, redirect to the existing thank-you page, map its Payment Link ID to the member package in the app, deploy, use once, and deactivate immediately after verification. Obtain user confirmation before creating this live test link.
- [x] Obtain user confirmation for the private one-use $0 test-link fallback. The test link must remain unshared, collect full name and phone, use the existing FUEL 42 thank-you page, and be deactivated immediately after the single controlled checkout.
- [ ] Create a private one-time `FUEL 42 QA — Automation Test` Stripe product with one active $0 USD price and an associated Payment Link. The link must collect name and phone, use no automatic tax, set quantity to one, prohibit repeat completions, and redirect to the established FUEL 42 thank-you page.
- [ ] Complete the approved private test-link creation form after Stripe finishes loading it. The direct creation URL has opened but is still rendering; do not create or modify any public FUEL 42 product/link during this step.
- [x] Verify the private Payment Link creator is loaded and exposes an `Add new product` option plus controls for automatic tax, customer name, phone collection, payment limit, advanced settings, and post-payment behavior. Create the dedicated $0 QA product rather than select any active public FUEL 42 product.
- [x] Stage the dedicated product as `FUEL 42 QA — Automation Test` with a one-off $0.00 USD price. It will be added only to the confirmed private test link; no existing public product, price, or Payment Link has been altered.
- [ ] Resolve Stripe’s zero-price validation in the private test-link creator. Stripe rejected the staged product with `Amount is required` while the amount field displays $0.00, so no QA product or link has been created. Confirm supported zero-dollar Payment Link behavior before proposing a paid-and-refunded alternative.
- [x] Confirm the root cause from Stripe’s official no-cost-orders documentation: Payment Links only support $0 products and 100%-discounted orders by default for Stripe accounts created after Aug. 17, 2023. Older accounts require account-level no-cost orders to be enabled under Checkout settings. This account’s previous coupon behavior and the $0 price validation are consistent with that capability being disabled.
- [ ] Obtain explicit approval before enabling the Stripe account-level no-cost-orders setting. It allows $0 Payment Link and pricing-table checkouts across the account; Stripe provides a three-day period in which the setting can be turned back off, after which it remains enabled.
- [ ] After approval, enable no-cost orders in Stripe Checkout settings, confirm the capability is active, and resume the already approved private one-use FUEL 42 QA link rather than collecting payment and refunding it.
- [x] Locate Stripe Checkout’s account-level `No-cost orders` control and confirm it is currently disabled (`Enable` button shown). User has explicitly approved enabling this capability; proceed and verify the saved state before resuming checkout testing.
- [x] Enable Stripe no-cost orders and verify the Checkout settings now state that free products and 100%-off coupons are accepted. Stripe displays an Undo option through Sep. 3, 2026 at 3:08 p.m.; no product, public Payment Link, or price was changed by this setting.
- [ ] Retest the existing active $139 FUEL 42 member Payment Link with the still-unused private `FUEL42QA2026` code. If it now reduces the total to $0, use that existing webhook-mapped link for the controlled checkout and cancel the unnecessary private $0 test-link fallback.
- [x] Retest the existing $139 member Payment Link after enabling no-cost orders. `FUEL42QA2026` now applies successfully as a 100% discount, the checkout displays $0.00, removes payment-method collection, and offers `Complete order`; the earlier invalid-code behavior is resolved without changing the product or Payment Link.
- [x] Cancel the private $0 test-product/link fallback because it is no longer needed. The QA product draft was never created, no new Payment Link ID needs to be added to the app, and the existing production member link remains the controlled test source.
- [ ] Enter the approved test contact details into the now-zero-dollar checkout, complete exactly one order, and verify the redirect and downstream webhook behavior.
- [ ] Complete the staged zero-dollar order: the approved recipient email and test participant name are entered, the private code is applied, and the total is $0. Enter the approved mobile number, review the completed fields, and submit exactly once.
- [x] Complete exactly one controlled $0 FUEL 42 member order using the approved email, test participant name, and mobile number. Stripe accepted the order without collecting a payment method and redirected successfully to `https://fueldifferent.app/fuel-42/thank-you`.
- [ ] Verify the completed no-cost Stripe order, FUEL 42 enrollment row/admin roster entry, Gymnetics contact update and `FUEL 42 — Registered` tag, internal notifications to Kelly and Coach Abby, welcome SMS, Gymnetics booking email, and direct fallback booking email.
- [x] Verify Stripe recorded exactly one $0.00 no-cost order for `FUEL 42 — Member Challenge Access` at Aug. 31, 3:08 p.m. The order has a live Checkout Session ID and no second no-cost order appears in the account list.
- [x] Verify the controlled test participant appears in the Fuel Different admin FUEL 42 roster as `Kelly Green - FUEL 42 Test`, with the expected email/phone, `FUEL 42 — Member Challenge Access`, purchase date Aug. 31, 2026, and `Purchased` / paid-awaiting-setup status. The roster successfully created one enrollment from the zero-dollar Checkout Session.
- [ ] Review the roster amount display: it shows the package list price `$139`, not the Stripe amount actually collected `$0`. Confirm whether this is intentionally showing package value or should be relabeled/updated to avoid implying the test participant paid $139.
- [ ] Fix the FUEL 42 webhook’s zero-dollar amount handling. The enrollment API confirms the controlled no-cost checkout was incorrectly stored as `amount_cents: 13900` because `session.amount_total || package.amountCents` treats a legitimate zero as false. Replace the fallback with nullish handling so completed 100%-discount orders store `$0` while genuinely absent totals still use the package amount.
- [ ] Add safe structured logging for the Gymnetics handoff response status and a non-sensitive response summary. Do not log the webhook URL or participant PII. Preserve direct booking-email fallback behavior.
- [ ] Correct the existing controlled QA enrollment amount from 13900 to 0 after the code fix is deployed, then confirm the roster/API reflects the actual amount collected.
- [x] Implement the zero-dollar amount fix with nullish fallback and add safe Gymnetics response diagnostics containing only HTTP success/status and a truncated response summary. Production build passes under `NODE_ENV=production`; no dependency files were changed.
- [ ] Commit and push the webhook fix, wait for the production deployment to succeed, then replay the existing Stripe `checkout.session.completed` event once. The idempotent enrollment check will update the amount to $0 without re-sending participant communications or re-entering Gymnetics.
- [ ] Verify the enrollment’s Oct. 31 access expiration after the setup token is claimed; the pre-setup roster screen does not display the expiration date directly.
- [ ] Verify Gymnetics created/updated the test contact, attached `FUEL 42 — Registered`, and recorded workflow execution for the internal notification, welcome SMS, and booking email.
- [ ] Gymnetics contact search finds one existing contact for `kelly@crossfitironflag.com`, named `Kelly McGriff-Culver`. The list still displays the pre-existing phone `(630) 716-9330` rather than the test checkout number, and the visible tag summary shows `won`, `sold`, `lead`, plus 20 hidden tags. Open the detailed record to determine whether `FUEL 42 — Registered` was added and whether the workflow intentionally preserved existing contact identity/phone fields.
- [x] Open the matched Gymnetics contact and expand all 23 tags. `FUEL 42 — Registered` is absent. The record still has its existing name and phone, and the conversation timeline has no new FUEL 42 email/SMS activity from the 3:08 p.m. test order. This indicates the purchase webhook did not successfully execute the published Gymnetics workflow actions, even though the Fuel Different enrollment was created.
- [x] User confirmed receipt of an email and the Gymnetics internal notification, but no welcome SMS. This proves the inbound webhook reached the published workflow and at least the internal-notification action executed. Because the direct Fuel Different booking-email fallback remained active, the received participant email is not yet proof that Gymnetics email executed.
- [ ] Diagnose the contact-action branch: Gymnetics matched the purchase email to an existing contact with a different saved phone number and did not add the FUEL 42 tag. Check whether SMS DND is active, whether Create/Update Contact mapped incoming phone/name fields correctly, and whether tag/SMS actions failed or targeted the pre-existing phone.
- [x] Identify the mapping error: the Create/Update Contact action used `Contact.Email`, `Contact.Full Name`, and `Contact.Phone`, which referenced the anonymous webhook enrollment rather than inbound payload fields. User remapped these to Inbound Webhook `email`, `full_name`, and `phone`, saved, and republished.
- [x] With explicit user approval, send one direct diagnostic webhook to the republished trigger using the approved test email/mobile. The endpoint accepted the request without an HTTP/network error. Verify downstream contact update, FUEL 42 tag, internal notification, booking email, and welcome SMS next.
- [x] Verify the remapped diagnostic attached to the real Gymnetics contact: its tag count increased from 23 to 24, and the newly applied tag is `fuel42fall26`. The workflow email and SMS now appear in the contact conversation timeline.
- [x] Verify SMS DND is not the blocker. The contact has DND All, Email, Text Messages, Calls, and Inbound SMS all set to false.
- [ ] Correct the remaining phone update issue. The workflow matched the contact by email but preserved the existing `(630) 716-9330` phone instead of applying inbound `+1 217-242-4648`; the SMS timeline entry shows a warning/`Try Again` state and did not reach the approved test number. Add an explicit `Update Contact Field` action for Phone immediately after Create/Update Contact, mapping it to Inbound Webhook `phone`, then retest.
- [x] User added `Update Contact Field` for Phone immediately after Create/Update Contact, mapped it to Inbound Webhook `phone`, and republished the workflow.
- [x] With explicit approval, send the final live diagnostic webhook through the republished workflow. The endpoint accepted the request without an HTTP/network error; verify the contact now shows `+1 217-242-4648` and the new SMS no longer has a warning/`Try Again` state.
- [ ] Confirm the final diagnostic updated the Gymnetics contact phone to `+1 217-242-4648`, created a successful SMS action, and reached the user’s mobile device.
- [x] Inspect the failed SMS message details. Gymnetics/Twilio attempted to send **from** `(630) 716-9330` **to** `(630) 716-9330`, so the message failed because the matched owner contact’s phone equals the sending business number. The explicit phone-update action did not replace that existing contact value during the diagnostic.
- [ ] Validate the workflow once with a clean test contact/email so Create Contact starts with the approved `+1 217-242-4648` mobile number. This distinguishes a one-off owner-contact data conflict from a general SMS workflow problem; delete or label the test contact after verification.
- [x] With explicit approval, send one final clean-contact diagnostic using the labeled Gmail alias `kagreen2+fuel42smsfinal@gmail.com` and approved mobile `+1 217-242-4648`. The Gymnetics endpoint accepted the request without an HTTP/network error; verify the new contact, `fuel42fall26` tag, email, internal notification, and SMS delivery next.
- [x] Gymnetics resolves the clean alias to a contact labeled `Kelly Green - FUEL 42 SMS Test` with the correct `(217) 242-4648` phone. The record has 19 tags and recent activity from the diagnostic. Expand tags and inspect the conversation timeline to verify `fuel42fall26` and the final SMS status.
- [x] Expand the clean test contact tags and confirm `fuel42fall26` is present.
- [x] Confirm the clean contact conversation shows the internal notification, welcome SMS, and booking email at 5:08 p.m. The SMS has no `Try again` failure state, unlike the owner-contact tests, and the contact’s active phone is the approved `(217) 242-4648` number. Treat the workflow delivery path as validated; user can provide final handset-level confirmation if desired.
- [ ] Decide whether `fuel42fall26` is the intended participant-list tag or replace it with the originally specified `FUEL 42 — Registered`. Use the same chosen tag for the later Monday office-hours SMS workflow.
- [x] User confirmed `fuel42fall26` is the official participant tag to use for the purchase workflow and the later Monday office-hours SMS workflow.

## Gymnetics FUEL 42 campaign email

- [ ] Write one campaign email suitable for both Iron Flag members and non-members, with clear package segmentation rather than implying every package includes classes.
- [ ] Include the verified dates (Sept. 14–Oct. 25, 2026), Sept. 13 registration deadline, two InBody scans, Fuel Different app access, weekly 30-minute Zoom coaching calls, inclusive GLP-1-friendly and hormone-change-aware positioning, and the no-refund/future-credit-at-discretion note.
- [ ] Present the four accurate package options: $139 active-member access; $249 nutrition plus unlimited CrossFit/HYROX/Iron Pump classes; $499 nutrition plus six 45-minute PT sessions without classes; $799 nutrition plus twelve 45-minute PT sessions without classes.
- [ ] Build a responsive, table-based email HTML file with inline CSS, white body background, email-safe typography, accessible contrast, and separate tracked CTA buttons using the four live Stripe links.
- [ ] Validate that the HTML contains no scripts, forms, unsupported external CSS, accidental dark body background, or missing links; provide a subject line, preview text, and plain-text fallback alongside the copy-paste HTML file.
- [x] Create the combined member/non-member FUEL 42 campaign email as `marketing/fuel42/fuel42-gymnetics-campaign-email.html`, with a white background, table-based responsive layout, inline styles, complete four-package breakdown, and four accurate live Stripe CTAs.
- [x] Create the companion plain-text fallback and usage notes. Validation confirmed exactly four live Stripe checkout URLs, the Sep. 13 registration deadline, a white email background, and no scripts or forms.
- [ ] Identify the source of the received participant email by its sender or message details before switching `FUEL42_GYMNETICS_EMAIL_ACTIVE` to true.
- [ ] Inspect the published FUEL 42 workflow execution/enrollment history and the inbound-webhook trigger status to determine whether the production POST reached Gymnetics, was rejected before enrollment, or entered with unmapped contact data.
- [x] Confirm production received the controlled Stripe event: Vercel recorded `POST /api/billing/webhook` with HTTP 200 at the test time. No runtime error or Gymnetics exception was logged. Because the app only logs an exception for network failures and otherwise treats any HTTP 2xx as success, this confirms the enrollment handler completed but does not prove Gymnetics enrolled the contact.
- [ ] Inspect Gymnetics workflow execution history for the test time. If no execution exists, add temporary structured logging for the outbound Gymnetics response status/body or send one separately approved diagnostic webhook after verifying the trigger URL and published status.
- [ ] Gymnetics Automation > Workflows list now loads successfully and exposes workflow status, total enrolled, active enrolled, and stats columns. Search for the FUEL 42 workflow and inspect its enrollment/history count rather than relying on the blank direct editor canvas.
- [ ] The workflow list renders, but its embedded search/table controls remain unstable to remote input and the direct workflow canvas returns a blank shell. HighLevel’s official path is Workflows → open workflow → Execution Logs → View Details; use that path when the control is available or have the user open Execution Logs locally.
- [ ] Verify the protected production `FUEL42_GYMNETICS_WEBHOOK_URL` still contains the final saved inbound-trigger endpoint. The app’s current code returns `false` silently for missing environment configuration or non-2xx webhook responses, and only logs thrown network errors; the lack of a Gymnetics log therefore does not rule out an HTTP rejection.
- [ ] Add the private test Payment Link ID to the app’s server-side `FUEL42_PACKAGES` mapping as the existing `member` package before attempting checkout. Verify TypeScript/build, push the mapping to `kagreen2/fueling-app`, and wait for the Vercel deployment to become Ready.
- [ ] Complete exactly one controlled test checkout with the approved recipient contact, then immediately deactivate the private test Payment Link and verify that no live public FUEL 42 link or recurrent billing configuration changed.
- [x] Confirm the active member Payment Link object ID is `plink_1U95KyFd19nnAKLW98iJQEQE` in the same Stripe account and its overview points to the visible product `FUEL 42 — Member Challenge Access` at $139. The dashboard HTML does not expose the underlying `prod_` or `price_` identifiers, so open the linked product record or Stripe object inspector next.
- [x] Open Stripe’s read-only object inspector for the active member Payment Link. The raw object confirms `active: true`, `allow_promotion_codes: true`, and the correct thank-you redirect. Extract the line-item price and product IDs next and compare them with the catalog record.
- [x] Confirm through Stripe Workbench Inspector that the raw member Payment Link object is active, belongs to the current account, allows promotion codes, and has no application fee or Connect application attached. The unexpanded object does not include line-item IDs; query the Payment Link’s line-items endpoint in read-only mode next.
- [x] Query `payment_links list_line_items` successfully in Stripe’s read-only Workbench shell and confirm the active link has one line item. Expand that line item and extract its nested `price.id` and `price.product` values next.
- [x] Expand the single Payment Link line-item object in Stripe Workbench. The response confirms adjustable quantity is disabled; maximize the read-only Workbench panel or query selected fields directly so the nested price and product identifiers can be captured without further page scrolling.
- [x] Confirm Stripe Workbench accepts the expanded line-items request with `--expand=data.price.product`; the visual JSON viewer still collapses nested fields and repeated scrolling does not expose them reliably. Stop scrolling and use targeted documentation/API inspection instead.
- [ ] Determine the least disruptive correction: repair the existing Payment Link if possible, or create a replacement $139 member Payment Link with the same one-time/no-tax/name/phone/quantity/redirect settings. Do not activate a replacement until its new Payment Link ID is added to the app webhook mapping, built, pushed, deployed, and the public FUEL 42 page is updated.
- [ ] Preserve all existing live FUEL 42 links and codes during diagnosis; do not delete, deactivate, or repoint the $139 link until the replacement path is verified end to end.
- [ ] Apply the still-unused private code to another active FUEL 42 Payment Link without submitting checkout. This will determine whether the rejection is specific to the $139 member link or affects promotion-code validation across the account.
- [ ] Shorten the private test coupon name to Stripe’s 40-character maximum and resubmit. Stripe rejected the first submission only because the internal coupon name exceeded that limit; no coupon or promotion code was created.
- [ ] Test a live participant setup link through the complete claim and onboarding flow.
- [ ] Configure and test a Gymnetics/GoHighLevel workflow that triggers after a successful FUEL 42 Stripe payment and emails the InBody scan and app-setup booking link.
- [ ] Apply the `FUEL 42 — Registered` contact tag after successful payment and use it to maintain the participant list.
- [ ] Create a separate Monday 1:15 p.m. Central Time office-hour Zoom SMS reminder workflow for tagged FUEL 42 participants through October 26, 2026.
- [ ] Treat Stripe’s `no_payment_required` status as a successful FUEL 42 checkout only when a valid 100%-off payment-link session is completed, so the approved `FUELFREE2` test can enroll correctly.
- [x] Add the optional secure `FUEL42_GYMNETICS_WEBHOOK_URL` bridge; direct email remains a fallback if Gymnetics is temporarily unavailable.
- [x] Configure the supplied Gymnetics inbound webhook as a protected `FUEL42_GYMNETICS_WEBHOOK_URL` production variable in Vercel. The earlier draft endpoint was replaced with the final saved trigger after its workflow actions were completed; the associated production redeployment completed successfully and is Ready.
- [ ] Adjust the app handoff so the direct booking email remains active until the Gymnetics workflow is fully configured, preventing a purchaser from receiving duplicate booking emails during the draft stage.
- [x] Confirm authenticated access to the Iron Flag Fitness – Westmont Gymnetics Automation workspace.
- [x] Document the two FUEL 42 Gymnetics workflows, audience tag, trigger boundaries, timing, and final activation requirements in `FUEL42-GYMNETICS-WORKFLOWS.md`.
- [x] Create a draft Gymnetics workflow shell for FUEL 42 purchase confirmation and consultation booking (workflow ID `a3e11e55-c9ee-4711-9a0f-f8380170bfe1`). The remote browser bridge reaches the builder URL but its screenshot/semantic extraction is currently blank, so configuration will use a guided handoff if the controls do not recover.
- [x] Configure the four draft Gymnetics actions after the inbound trigger: Create/Update Contact, add the `FUEL 42 — Registered` tag, internal notification to Kelly and Coach Abby, and the consultation-booking email. The trigger remains unpublished pending a controlled end-to-end test.
- [ ] Publish the completed Gymnetics purchase workflow for the user-approved controlled test. The linked Gymnetics editor currently loads but does not expose usable remote controls, so complete publication through the user’s browser if this condition persists.
- [x] Send a single, non-production mapping-reference test request to the new Gymnetics endpoint. Gymnetics returned HTTP 200 with “Success: test request received”; the test payload uses a reserved `invalid.example` email and a test-only package value.

## Chris Tavonatti macro-calculation audit

- [ ] Locate Chris Tavonatti’s athlete profile and identify the profile ID, current displayed macro values, and recent meal-log records without changing account data.
- [x] Confirm authenticated read access to the Fuel Different admin dashboard and its 106-user directory. Proceed to the Users view and search for Chris Tavonatti; do not change his profile or targets.
- [x] Identify the account as `Christopher Tavonatti` (`sigmarshammer13@yahoo.com`), active athlete, joined Aug. 21, 2026, with profile ID `3d1a6c07-0be5-47f9-9852-8a4ce9a21a36`. Opened the read-only coach athlete view; nutrition data is still loading.
- [x] Load Chris’s coach dashboard. It displays daily targets of 2,504 calories, 177 g protein, 305 g carbohydrates, and 64 g fat. Eleven logged days show daily calories from 715 to 2,256 and protein from 65 g to 179 g; the displayed 11-day averages are 1,593 calories and 136 g protein. Next inspect meal-level records and independently recompute these daily and average totals.
- [ ] Access meal-level records through the admin Nutrition data source. The visible Nutrition tab timed out when opened remotely, so inspect its implementation and use the same authenticated read path rather than changing Chris’s account.
- [ ] Recalculate each recent meal and daily total directly from the stored calories, protein, carbohydrates, fat, quantity, serving size, and portion-scaling fields.
- [ ] Compare the independently recalculated totals with the athlete dashboard and coach-facing totals to identify any arithmetic, rounding, date-boundary, or aggregation discrepancy.
- [x] Query all of Chris Tavonatti’s stored meal records through a temporary admin-only read endpoint and independently sum each day. Every displayed coach-summary calorie total, protein total, and meal count exactly matches the underlying meal rows for Aug. 22 through Sep. 1; no dashboard summation, rounding, or date-grouping error was found.
- [x] Confirm the meal records store final calories, protein, carbs, and fat directly. They do not contain quantity or serving-size scaling fields; each logged meal is counted once at the saved macro values. Daily calories are summed from the stored calorie field rather than recalculated from 4/4/9 macros.
- [ ] Trace the app code that creates manual/photo meal entries, scales portions, saves edits, relogs meals, and aggregates daily macros.
- [ ] Check whether any identified discrepancy is specific to Chris’s entry method or reproducible across other records using the same calculation path.
- [ ] Report only the technical calculation findings and any software correction required; do not recommend or modify Chris’s nutrition targets.
